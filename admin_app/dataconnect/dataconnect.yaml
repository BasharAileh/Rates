specVersion: "v1beta"
serviceId: "admin_app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "admin_app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
